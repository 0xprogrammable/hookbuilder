# Programmable v4 Builder v0.9.2

This release extends the existing complete-project workflow with broader local implementation, packaging, handoff,
and benchmark infrastructure while keeping launch, approval, external execution, and publication authority separate.

## Complete projects continue into implementation

- A request can move from an accepted design into implementation without matching a bundled reference profile.
- For custom tradable projects, a Foundry contract tree can be paired with exactly one selected web, service, or game
  source tree and preserved as inert project input. The application-surface label describes owner intent; it is not a
  claim that the application has been executed, audited, or deployed.
- Generated plans and receipts bind accepted regular-file bytes and the complete caller-supplied contract configuration,
  including dependencies, lock, remappings, Foundry settings, extra safe root configuration and Git modes. Omitting the
  new configuration options retains the v0.9.1 convenience default for compatibility. Candidate commands remain
  `NOT_RUN` until separately trusted execution evidence exists.
- Configuration, source, test, and application trees enforce incremental entry, directory, depth, file, and byte limits
  before recursive traversal or retention.

## Exact no-write submission handoff

- `handoff preview` produces lossless canonical handoff bytes for complete no-market, tradable, and multi-surface
  projects.
- The handoff validates exact supplied central-policy and schema preimages, derives their active build requirements,
  and binds their recorded Git identities together with the protected Submit base, application directory, source
  revision, project receipt, complete surface inventory, and intended draft pull-request identity. It does not
  re-resolve current remote state.
- Preview performs no network or filesystem mutation. The portable write path rechecks its confirmation and then
  fails closed until a reviewed descriptor-bound external writer exists.

## Benchmarkable quality

- A versioned public journey corpus covers natural English and German requests, the reported design-to-implementation
  regression, adjacent non-trigger cases, malformed inputs, missing tools, unavailable authority, and adversarial
  repository behavior. Its append-only v3 correction expects the Mizu design-only turn to remain `NOT_ACTIVATED` and
  the explicit implementation continuation to become `ACTIVATED`.
- The comparison harness keeps expected outcomes and rubrics out of subject requests, binds adapter requests and
  evidence, and records activation, references, repository output, efficiency, effects, and independent-judge
  results. It bounds workspace traversal, secret scanning, adapter sidecars, metrics, effects, run count, and aggregate
  evidence before materializing a scorecard.
- Structural and fake-adapter checks are local regression evidence only. Provider-backed comparisons require reviewed
  external adapters, separately trusted isolation, and independently established judge evidence; none are implied by
  this source release.

## Smaller portable installation

- One manifest now defines the portable runtime closure used by Skill installation, plugin generation, release
  artifacts, size checks, and installed verification.
- Development tests remain in the full repository suite but are not copied into ordinary Skill installations.
- The installed package retains deterministic runtime smoke checks for CLI, routing, scaffolding, bounded inputs, and
  frozen compatibility behavior.
- Against the exact v0.9.1 Skill tree, the portable source drops from 686 files / 10,722,006 bytes to 618 files /
  8,656,525 bytes: 68 fewer files (-9.91%) and 2,065,481 fewer bytes (-19.26%).
- The final source inventory is mechanically bound to 342 production modules and 10 local test files under
  `evals/tests`; these counts describe source coverage only, not execution outcomes.
- The unchanged reference-kernel source inventory contains 54 unit, one fuzz and three invariant test functions;
  execution can be claimed only by the final revision-bound release receipt.

## Sandbox integration boundary

- Host operators receive a deterministic, networkless container invocation plan and explicit evidence contracts.
- Source archives are built deterministically from exact Git tree blobs and executable modes, not `git archive`; export
  attributes cannot omit or rewrite bytes, unsupported Git modes fail closed, and finalization cannot replace a racing
  output path. Combined tree entries, unique retained blobs, and archive bytes are capped before allocation.
- The portable package does not ship a runner image, production launcher, trust root, signing key, or host authority.
  A caller-created key or locally fabricated receipt cannot establish completion.
- Real isolation, resource enforcement, teardown, trusted signing, and completion import remain external and must be
  independently verified.

## Authority boundary

Local source generation and handoff preparation remain `NOT_SUBMITTED` and `NOT_APPROVED`. Policy satisfaction and
passing tests are not an audit or automatic approval. Protected Submit a Launch and Website services retain
independent review, acceptance, deployment, routing, discovery, signing, real-funds, and launch authority.
Contributors author and push candidate branches; a separate current CODEOWNER reviews the exact head and retains
squash-merge, tag, and publication authority.
The pre-1.0 versioning policy now records that a patch may include optional backward-compatible capabilities only when
existing workflows gain no required input and no capability contract changes incompatibly.

The bundled state retains `publicationStateVerified: false`. Source, branch, local tests, or a release candidate do not
prove that the public `v0.9.2` tag, immutable GitHub Release, assets, protected CI, or installed bytes exist.
