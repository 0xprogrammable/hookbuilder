# Programmable v4 Builder v0.6.0 local candidate

Status: `UNRELEASED_LOCAL_CANDIDATE`.

This file describes local source intent. It is not a GitHub release, release-candidate verdict, latest-version claim,
stable-version claim, publication receipt, audit, approval, deployment, or live-state record. Stable public installation
guidance remains `v0.5.1` until the complete release policy closes for one immutable `v0.6.0` revision and the owner
separately authorizes publication.

## Candidate scope

- Route source-receipted provider knowledge only after a provider capability is confirmed. Treat every source as
  filtered guidance, never inherited safety, provider availability, compatibility, approval, or production evidence.
- Target a shorter useful journey with deferred specialist context and concise primary outcomes.
- Target stricter bounded execution and authority-denied behavior without weakening semantic, safety, or release gates.
- Require measured end-to-end context, output, tool-call, retry, latency, and reference-use evidence before making an
  efficiency claim.

The provider-knowledge source is present in this candidate. UX, executor-safety, simplification, and efficiency work is
being integrated in separate lanes; this document does not claim those lanes are complete or included in final bytes.

## Generator-backed source inventory

These are source inventories, not execution results:

- The maintainability size generator discovers 320 production JavaScript modules across its two configured roots.
- The generated Contract Registry contains 49 schema contracts and 24 validator closures comprising 1,011 transitive
  module bindings across 172 distinct modules.
- The release-evidence source inventory finds 54 unit, one fuzz and three invariant Solidity functions in the Fee V2
  kernel, 58 functions in total.
- The repository gate dynamically discovers 9 `evals/tests/*.test.mjs` files. A filename count is not a model run or
  test-pass receipt.

All counts must be regenerated from the final integrated candidate. A source change invalidates these values until the
version, Registry, maintainability, release-evidence, documentation-link, and plugin checks pass again.

## Evidence still required

- one clean immutable commit, repository tree, and portable skill tree containing the complete intended change set;
- the complete repository and release rehearsal on those exact bytes;
- independent natural-language forward tests and the required external model/judge evidence;
- trusted sandbox, comparator, holdout, fork, multi-host, public CI, and security-review evidence required by policy;
- exact owner authority for tag, release, publication, and any announcement; and
- post-publication resolution of the public tag, artifacts, checksums, installed package, and default installation.

Until those gates close, the supported state is local source development only and `releaseCandidate: false`.
